# Ambiente de Execução
# Valores: development, staging, production
ENV=development

# Nível de Log
# Valores: debug, info, warn, error
# - debug: Mostra todos os logs (verbose)
# - info: Logs informativos e acima (padrão produção)
# - warn: Apenas avisos e erros
# - error: Somente erros
LOG_LEVEL=debug

# Porta do Servidor
# Padrão: 8080
# Nota: Em produção (Railway, Heroku), esta variável é definida automaticamente pela plataforma
PORT=8080

# Database
# PostgreSQL connection string
# Formato: postgres://usuario:senha@host:porta/nome_banco?sslmode=disable
# No Railway, esta variável é definida automaticamente
DATABASE_URL=postgres://user:password@localhost:5432/receitas_db?sslmode=disable
